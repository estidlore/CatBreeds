import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  image: {
    aspectRatio: 1,
    height: "auto",
    marginBottom: 12,
    width: "100%",
  },
  text: {
    marginVertical: 8,
  },
});

export { styles };
