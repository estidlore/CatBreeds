import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  backButton: {
    aspectRatio: 1,
    position: "absolute",
    height: "100%",
  },
  backImage: {
    height: "100%",
    width: "100%",
  },
  box: {
    marginBottom: 12,
  },
});

export { styles };
